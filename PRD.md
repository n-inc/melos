# Melos CLI TUI リデザイン

## 概要

Melosのターミナル出力を、現在のストリーム型出力からフルスクリーンTUI（Terminal User Interface）に再構築する。Claude Code CLI / Codex CLIのようなモダンなCLI AIエージェントの見た目と操作感を実現し、2つのAIエンジン（Claude / Codex）の実行状態を統一的に表示する。

## 背景・動機

現在のMelosは、ANSI色・ボックス描画・スピナーによるストリーム型出力を採用している。これには以下の課題がある：

- **エンジン/モデルの状態が流れてしまう**: イテレーションヘッダーにエンジン情報があるが、スクロールすると見えなくなる
- **Claude実行時とCodex実行時の見え方が異なる**: ストリーミングコールバックの種類や出力形式が統一されていない
- **タスク全体の進捗が俯瞰しにくい**: 個別イテレーションの情報は出るが、全体の進行状況が常時見えない
- **CLI AIエージェントとしての"らしさ"が弱い**: 現代のCLI AIツール（Claude Code, Codex CLI等）と比べて、操作感やステータス表示が古い印象

## ユーザー体験

### 画面レイアウト

フルスクリーンTUIで、以下の3領域に分割する：

```
┌─────────────────────────────────────────────────────────────┐
│ ステータスバー（固定・2行）                                    │
│  1行目: エンジン・モデル・フェーズ・経過時間                     │
│  2行目: タスク進捗バー・現在のタスク名                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ メイン出力エリア（スクロール）                                  │
│  エージェントの出力・ツール実行結果・差分表示等                   │
│                                                             │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ 入力エリア（固定）                                            │
│  melos> ユーザー入力                                         │
└─────────────────────────────────────────────────────────────┘
```

### ステータスバー（常時固定表示）

ユーザーが常に把握できるべき6つの情報を、2行に配置する：

**1行目: 実行状態**
```
▸ Manager  ·  Claude (opus)  ·  effort: max  ·  2m 15s
```

- **フェーズ**: `▸ Manager` / `▸ Worker` / `● 待機中` / `⚠ 回答待ち`
- **エンジン+モデル**: `Claude (opus)` / `Codex (gpt-5.3-codex)` — エンジン名で色分け（Claude=一色、Codex=別色）
- **設定**: `effort: max` / `reasoning: high` （エンジンに応じた設定値）
- **経過時間**: 現在のフェーズの経過時間

**2行目: タスク進捗**
```
3/8 ████░░░░ 38%  ·  [T-003] auth module 実装
```

- **進捗**: `完了数/総数` + プログレスバー + パーセント
- **現在タスク**: タスクID + 説明（端末幅に合わせて切り詰め）

### メイン出力エリア（スクロール）

エージェントの出力を構造化して表示する。Claude実行時もCodex実行時も、同じ表示形式に統一する：

**エージェント思考/メッセージ**: 通常テキストとしてストリーミング表示

**ツール実行**: カード形式で表示し、長い出力は省略→展開
```
● Read src/auth.ts (245 lines)
● Bash: npm run test
  ✓ 完了 (exit 0, 3.2s)
  └─ 出力 12行（展開: e）
● Write src/auth.ts (+45 -12)
  @@ -10,5 +10,8 @@
  + import { validateToken } from './utils';
  - const oldAuth = require('old-lib');
  └─ +8行（展開: e）
```

**イテレーション区切り**: 薄いセパレーターで区切る
```
── イテレーション 2 完了 (1m 45s) ──────────────────
```

**イテレーションサマリー**: 完了時に簡潔に表示
```
✓ T-003 passed  ·  変更: src/auth.ts (+45 -12), src/utils.ts (+20)
```

### 入力エリア（固定）

- プロンプト `melos>` を常時表示
- ステアリング入力をいつでも受け付ける
- 入力のフィードバック:
  - `[steer] 送信しました` — 即時送信（Codex実行中）
  - `[steer] 保留 (1件)` — 次回Managerへ引渡し（Claude Worker中）
  - `[answer] 回答を送信しました` — エスカレーション回答時

### エスカレーション（質問）表示

Managerからの質問時、メイン出力エリアに目立つボックスで表示：

```
╭─ ユーザー確認 ──────────────────────────────╮
│ 認証方式はどちらを使いますか？               │
│                                             │
│  1. JWT（推奨）                              │
│  2. Session-based                           │
│  3. OAuth 2.0                               │
╰─────────────────────────────────────────────╯
```

ステータスバーのフェーズが `⚠ 回答待ち` に変わり、入力を促す。

### 起動時

起動時にセッション情報を簡潔に表示してから、実行を開始する：

```
melos v0.7.0
─────────────────────────────
PRD:     Feature implementation
タスク:   8件
Manager: Claude (opus, effort: max)
Worker:  Codex (gpt-5.3-codex, reasoning: high)
─────────────────────────────
```

### 完了時

全タスク完了時、ステータスバーを更新し、サマリーを表示：

```
ステータスバー: ✓ 完了  ·  5 イテレーション  ·  12m 30s

メインエリア:
✓ Melos 完了

タスク結果:
  ✓ T-001  型定義の追加
  ✓ T-002  API エンドポイント実装
  ✓ T-003  auth module 実装
  ...

引き継ぎ: HANDOFF.md を参照
```

### 中断・再開

**Ctrl+C 時**: ステータスバーに `⚠ 中断` を表示し、メインエリアに再開方法を案内
```
中断されました。再開: npx melos resume
```

### エラー表示

エラーはメインエリアに赤色で表示。ステータスバーのフェーズは `✗ エラー` に変わる。

### plainモード（--plain）

TUIを無効化し、現在と同等のストリーム型出力にフォールバック。CI環境や非TTYでは自動的にplainモードになる。

## 受入基準

1. `npx melos` 実行時にフルスクリーンTUIが起動する
2. ステータスバーにエンジン名・モデル・フェーズ・経過時間・タスク進捗が常時表示される
3. Manager実行中とWorker実行中で、ステータスバーのフェーズ・エンジン表示が正しく切り替わる
4. Claude実行時とCodex実行時のメイン出力が同じ形式で表示される
5. ツール実行（ファイル読み書き・コマンド実行）がカード形式で表示される
6. `melos>` プロンプトからステアリング入力ができる
7. エスカレーション（質問）がボックス表示され、回答できる
8. Ctrl+Cで中断した際にセッションが保存され、`npx melos resume` で再開できる
9. `--plain` フラグまたは非TTY環境でストリーム型出力にフォールバックする
10. 既存の全機能（run, resume, kill, dry-run, review-only）が動作する

## スコープ外

- ステータスバーのカスタマイズ（項目の並び替え・表示/非表示）
- トークン使用量・コスト表示（将来の拡張として検討）
- マウス操作対応
- テーマ・カラースキームの変更機能
